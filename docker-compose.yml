services:
  postgres:
    image: postgres:16-alpine
    container_name: cursor-consumption-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cursor_consumption
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
